name: Docker Image CI

on:
  push:
    branches: [ "backup-code-2025-06-06" ]
  pull_request:
    branches: [ "backup-code-2025-06-06" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
